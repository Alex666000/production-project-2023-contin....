import ThemeProvider from "./ui/ThemeProvider";
import {useTheme} from "./lib/useTheme";


export {
    ThemeProvider,
    useTheme,
};

/*
- index.ts (создаем в корне папки) --> импортируем все к себе из всей папки и экспортируем наружу то что импортировали
 */